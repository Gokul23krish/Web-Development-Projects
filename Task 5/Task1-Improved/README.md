# Task 1 – Improved (SEO, Accessibility, Performance)

This is a non-destructive improved copy of Task 1. It keeps the original visuals from `Task 1/styles.css` and adds semantic structure, skip link, meta (title/description/OG), favicon, and crawler files.

## Changes
- **SEO**: Unique title/description, canonical, Open Graph/Twitter, favicon.
- **Accessibility**: Semantic landmarks, skip link, focus-visible outline, ARIA labels for icons/controls.
- **Performance**: Kept single CSS to avoid FOUC, added `preconnect` hints. (No JS in Task 1.)
- **Crawling**: `robots.txt` and `sitemap.xml` added for this copy.

## Files
- `index.html`
- `assets/favicon.svg`
- `robots.txt`
- `sitemap.xml`
- `screenshots/` (place Lighthouse reports here)

## Lighthouse (run and place screenshots here)
- BEFORE: screenshot(s) from original Task 1
- AFTER: screenshot(s) from this improved copy

## Notes
- If deploying to a different domain, update canonical/OG URLs in `index.html` and `sitemap.xml`.
